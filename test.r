x1<-rep(1,10)

calculateW2 <- function(x) {
    
}

f <- function(x) {
    
    # Initialize object to return
    result <- c()
    
    # Calculate w1
    result[[1]] <- rep(1, length(x))

    return(result)
}


f(c(1,2,3,4,5))
